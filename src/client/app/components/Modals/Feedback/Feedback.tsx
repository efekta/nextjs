import s from './Feedback.module.scss';

export const Feedback = (): JSX.Element => {

    return (
        <div className={s.feedback}>
            <h2 className={s.title}>Модалка</h2>
        </div>
    );
};
